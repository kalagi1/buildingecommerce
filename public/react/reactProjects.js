const App = () => {
    const { useEffect, useState } = React;

    
    useEffect(() => {
        
    },[])

    return (
        <div>
            <h1>Merhaba, Laravel ile CDN React!</h1>
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('react_projects'));